# Project Rules

## 不确定时必须确认

- 对需求、预期行为、实现方式、设计取舍、兼容性、数据格式、依赖变更或发布流程有任何不确定时，必须先向用户确认，不得自行假设后继续执行。
- 提出确认问题时，需要说明当前已知信息、具体不确定点，以及不同选择可能带来的影响。
- 在执行删除、覆盖、迁移、重置或其它可能造成不可逆影响的操作用前，如果用户没有明确授权，必须先确认。
- 可以先通过代码、文档、测试和本地命令查证事实；查证后仍无法唯一确定答案时，必须停止相关改动并等待用户确认。
- 只有在请求和验收标准明确，且不存在影响实现结果的未决问题时，才可以自主完成修改和验证。

## editor-core 分层
- editor-core 不准 import 应用层（modules/ui/services）或第三方解析库。
- 改 editor-core 后运行 `cd frontend && npm test`，其中包含 editor-core 分层门禁。
- shadow / dev-tools 不进入生产入口。
